[pytest]
# Test discovery — keep narrow so it doesn't try to import the legacy
# top-level scripts that depend on optional packages.
testpaths   = backend/tests
python_files = test_*.py
addopts     = -ra --strict-markers --color=yes
filterwarnings =
    ignore::DeprecationWarning
