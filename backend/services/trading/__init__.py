# coding: utf-8
# Phase 5.1 — Trading services package.
# Holds operator-side intelligence (thesis memory, future: trade journal,
# position manager, alert engine). Imports are guarded so a missing optional
# module never crashes the API.
